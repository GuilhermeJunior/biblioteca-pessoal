export class EndpointRoutes {
  private static readonly PATH = 'http://localhost:3000'
  public static readonly AUTHORS = `${this.PATH}/authors`;
  public static readonly BOOKS = `${this.PATH}/books`;
}
